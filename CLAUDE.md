# CLAUDE.md

## 角色设定

你是一名资深桌面应用架构师，专精于高性能图像处理软件的架构与实现。

## 技术栈

- Tauri v2（桌面框架，Rust 后端）
- React + TypeScript（前端）
- TailwindCSS（样式）
- pnpm（包管理）

## 核心功能需求

1. 极速加载：大图目录的快速索引与渲染
2. JPG/RAW 同名合并：将同名不同扩展名的 JPG 与 RAW 文件合并为单一条目展示
3. 单键快捷打分：通过键盘快捷键对照片进行评级
4. 调用系统回收站：删除操作使用系统回收站，而非直接物理删除
5. 连拍自动分组：按拍摄时间间隔自动将连拍照片归组
6. 双图同步对比缩放：并排对比两张图片，支持同步缩放和平移

## 协作原则

- 分步骤进行，每次仅完成当前下发的具体任务
- 不提前编写后续功能代码
- 每一步确保可编译、无报错、逻辑严密
- 优先编辑现有文件，避免不必要的新建文件
- 代码不加注释，除非意图非显而易见
- 不引入任务未要求的抽象或重构
